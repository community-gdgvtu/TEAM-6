# Server
NODE_ENV=development
PORT=4000
CLIENT_ORIGIN=http://localhost:5173

# MongoDB Atlas (Phase 1)
MONGODB_URI=mongodb+srv://saifmmark_db_user:<db_password>@cluster77.j1gayic.mongodb.net/?appName=Cluster77

# JWT (Phase 2)
JWT_SECRET=
JWT_EXPIRES_IN=7d

# Reserved for external admin-key integrations; current admin APIs use JWT role authorization.
ADMIN_KEY=
